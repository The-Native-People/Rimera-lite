---
title: How Rimera works
description: From a Python entry file to a native executable.
icon: Boxes
---

import {
  ArchiveIcon,
  ArrowRightIcon,
  BinaryIcon,
  CheckCircle2Icon,
  FileCode2Icon,
  Link2Icon,
  PlayIcon,
  ShieldCheckIcon,
} from 'lucide-react';

Rimera reads your Python entry file, checks the supported language features,
produces native machine code, and links a compact runtime into one executable.
You keep writing Python; the compiler handles the native details.

<Cards>
  <Card icon={<FileCode2Icon className="text-cyan-400" />} title="1 · Choose an entry file" description="Start with the Python file that should become the application entry point." />
  <Card icon={<BinaryIcon className="text-violet-400" />} title="2 · Compile" description="rimlite checks supported syntax and turns the program into native object code." />
  <Card icon={<Link2Icon className="text-amber-300" />} title="3 · Link" description="Rimera combines the native code and its runtime into one executable." />
  <Card icon={<PlayIcon className="text-emerald-400" />} title="4 · Run" description="Launch the executable directly, or let rimlite compile and run it for you." />
</Cards>

### What you get

<Cards>
  <Card icon={<ArrowRightIcon className="text-amber-300" />} title="Fast startup" href="/docs/reference/benchmarks">

  Native programs can start without launching a Python interpreter.

  </Card>
  <Card icon={<ArchiveIcon className="text-violet-400" />} title="Local build cache" href="/docs/using-rimera/configuration">

  Build intermediates live under `.rimera/`, beside the project they belong to.

  </Card>
  <Card icon={<ShieldCheckIcon className="text-emerald-400" />} title="Clear boundaries" href="/docs/reference/supported-python">

  Unsupported behavior fails clearly rather than silently running with different semantics.

  </Card>
  <Card icon={<CheckCircle2Icon className="text-cyan-400" />} title="One direct path" href="/docs/getting-started">

  Use the same `rimlite` command to compile an executable or compile and run it immediately.

  </Card>
</Cards>
