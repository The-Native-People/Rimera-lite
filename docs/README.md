# Rimera Lite docs

This is the Rimera Lite documentation site, built with [Fumadocs](https://fumadocs.dev/) on [Waku](https://waku.gg/).

Run development server:

```bash
bun run dev
```

Build for production with `bun run build`, then serve it with `bun run start`.
